"""Jira Cloud: the project per world version, work items owned through the
synthetic-owner select field with ``assignee`` left empty, world dates in the
generator-controlled ``Opened On`` / ``Resolved On`` fields, comments as plantable
content. Jira's ``created`` and ``resolutiondate`` are vendor time and stay hidden.
"""
